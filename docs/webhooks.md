# Webhooks (moved)

The user-facing webhooks guide is now canonical on the docs site.

- **Source of truth**: [`website/content/docs/webhooks.mdx`](../website/content/docs/webhooks.mdx)
- **Rendered**: `/docs/webhooks` on the deployed docs site (M6)

Edit the MDX under `website/content/docs`, not this file. Signature verification
is also shipped in the SDK (`verifyWebhookSignature`, see
[`sdk/src/webhook.ts`](../sdk/src/webhook.ts)).
