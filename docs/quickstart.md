# Quickstart (moved)

The user-facing quickstart is now canonical on the docs site.

- **Source of truth**: [`website/content/docs/quickstart.mdx`](../website/content/docs/quickstart.mdx)
- **Rendered**: `/docs/quickstart` on the deployed docs site (M6, Next.js/Vercel)

This stub keeps a single source of truth (the exact pain Comax Secrets removes).
Edit the MDX under `website/content/docs`, not this file. A minimal copy-paste
quickstart also lives in the repo [README](../README.md#quickstart).
